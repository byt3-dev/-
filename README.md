# Pleez dohnt ihnstahl vyruhsehs. :)
if u do i will find where u live :DDDDDDDDDDDDD)))))))))))))))))))))))))))))))HEHEHEHHEHE


```python
import gmaps
import ghvirus
import ghstats
from machine import drone

# we findz dem 
malwareinstaller = ghvirus.who("latest")
location = ghstats.location(malwareinstaller)

# eye in da sky
satelliteimg = gmaps(location).earth

# we saves da evidence
with open("satellite.png", "wb") as f:
    f.write(satelliteimg)

# send da spicy drones
drone.send_lc(location)
drone.deploy("unit_2") # 0 is taychon bomb (hit dem yesturday); out of unit_0
Use code with caution.
